# === Required: Deploy ===
# Bot / owner wallet (set as storage slot 0)
OWNER=0x...
# Deployer key — cast script and forge script --broadcast
PRIVATE_KEY=...

# === Optional: Fork tests / RPC ===
# Used by ArbExecutorAaveFork / ArbExecutorDebug and deploy scripts.
# Falls back to https://polygon-bor-rpc.publicnode.com when unset.
POLYGON_RPC_URL=
